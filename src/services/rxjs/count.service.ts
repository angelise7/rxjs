import { Injectable } from '@angular/core';
import { interval } from 'rxjs';
import { count, refCount } from 'rxjs/operators'

@Injectable({
  providedIn: 'root'
})
export class CountService {

  constructor() {
  
  }
}
